#include "shell.h"

/**
 * shell_read_line - reads and allocate string with line to read in stdin
 *
 * Return: pointer to a string with line to content.
 */

char *shelll_read_line(void)
{
	char buff[1024];
	char *l_ptr = NULL;
	size_ buf 
